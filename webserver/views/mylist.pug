extends layout

block content
  h1= title
  div#qrcode_list
    each item in qrcodes 
      a(href=`/qrcode/${item.qrcode_uuid}`)
        div.item-card
          <qrcode-image qrcodedata=#{item.base64img}></qrcode-image>
          p= item.title